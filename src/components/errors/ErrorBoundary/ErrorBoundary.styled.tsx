import styled from 'styled-components';
import { FlexColumnDiv } from '../../../GlobalStyles.styled';

export const ErrorPageContainer = styled(FlexColumnDiv)`
	width: 100vw;
	height: 100vh;
	justify-content: center;
	align-items: center;
`;
